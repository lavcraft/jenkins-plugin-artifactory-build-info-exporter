# Jenkins plugin for export BuildInfo from Jfrog build-info-extractor to build api

## Build

  ./gradlew build

## Binaries

Plugin hpi file must be located in build/libs directory. Grab it and use

